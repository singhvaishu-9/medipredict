{
  "name": "medipredict-ai-backend",
  "private": true,
  "scripts": {
    "start": "py -m uvicorn main:app --host 127.0.0.1 --port 8000",
    "dev": "py -m uvicorn main:app --reload --host 127.0.0.1 --port 8000"
  }
}
